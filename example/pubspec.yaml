name: example
description: AutoSizeText Example

version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.24.0-0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

  auto_size_text_pk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
